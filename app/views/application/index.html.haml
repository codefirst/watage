%h3 Watage: a simple online storage interface

.box
 %h4 Backend storage settings
 = link_to 'Setup here', authorize_path, :class => 'btn btn-primary'

.box
 %h4 Add attachment here
 = form_tag '/', :enctype => 'multipart/form-data' do
  = file_field_tag 'upload[file]'
  = submit_tag 'Upload', :class => 'btn btn-primary'
